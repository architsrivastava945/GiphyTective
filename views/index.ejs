<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>GiphyTective</title>
    <!-- Google Fonts: Inter -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;700&display=swap" rel="stylesheet">
    <!-- The path should be absolute from the public folder -->
    <link rel="stylesheet" href="/style.css">
</head>
<body>

    <header>
        <!-- FIXED: Wrapped H1 in a link to the homepage -->
        <a href="/" class="header-link">
            <h1>GiphyTective</h1>
        </a>
    </header>

    <main>
        <div class="search-wrapper">
            <form action="/search" method="POST">
                <input type="text" name="query" placeholder="Search for GIFs..." required autocomplete="off">
                <button type="submit">Search</button>
            </form>
        </div>

        <% if (gifs.length > 0) { %>
            <div class="gif-grid">
                <% gifs.forEach(gif => { %>
                    <div class="gif-item">
                        <img src="<%= gif.images.fixed_width.url %>" alt="<%= gif.title %>">
                        <!-- ADDED: A download button for each GIF -->
                        <a href="<%= gif.images.original.url %>" class="download-btn" download>
                            <!-- Simple SVG Download Icon -->
                            <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"></path><polyline points="7 10 12 15 17 10"></polyline><line x1="12" y1="15" x2="12" y2="3"></line></svg>
                        </a>
                    </div>
                <% }) %>
            </div>
        <% } else { %>
            <p>Use the search bar above to find GIFs!</p>
        <% } %>
    </main>

</body>
</html>

